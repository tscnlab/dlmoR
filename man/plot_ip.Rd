% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_profile.R
\name{plot_ip}
\alias{plot_ip}
\title{Add DLMO Inflection Point to the Plot}
\usage{
plot_ip(plot, profile_data, dlmoip)
}
\arguments{
\item{plot}{A ggplot object. The base plot of the melatonin profile.}

\item{profile_data}{A dataframe containing melatonin concentration data, with required columns:
\itemize{
\item \code{datetime} (POSIXct) – Time of measurement.
}}

\item{dlmoip}{A list containing the estimated \strong{inflection point} coordinates:
\itemize{
\item \code{x} (numeric) – The estimated inflection point time (in decimal hours).
\item \code{y} (numeric) – The estimated melatonin concentration at the inflection point.
}}
}
\value{
A ggplot object with the inflection point added.
}
\description{
This function adds a \strong{DLMO inflection point} as a distinct marker to an existing melatonin profile plot.
The inflection point is represented by a \strong{filled deep pink square}.
}
\details{
\itemize{
\item The \strong{inflection point} represents the DLMO time stamp, at which melatonin concentration begins to rise significantly.
\item The function \strong{converts} the inflection point \code{x} from \strong{decimal hours} back to \strong{POSIXct} for proper placement.
\item The point is \strong{styled} as a \strong{deep pink} filled square.
}
}
\examples{
\dontrun{
  plot <- ggplot2::ggplot(profile_data, ggplot2::aes(x = datetime, y = melatonin)) +
      ggplot2::geom_point()
  dlmoip <- list(x = 20.5, y = 3.2)  # Inflection point at 20.5 decimal hours, y = 3.2 pg/mL
  plot_ip(plot, profile_data, dlmoip)
}
}
