% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_profile.R
\name{plot_profile}
\alias{plot_profile}
\title{Generate a Melatonin Profile Plot}
\usage{
plot_profile(
  profile_data,
  show_threshold = TRUE,
  threshold = 2.3,
  show_segments = TRUE,
  show_parallelogram = FALSE,
  pll_result = NULL,
  show_roi = FALSE,
  roi_line_only = TRUE,
  roi = NULL,
  show_dlmoIP = TRUE,
  dlmo = NULL,
  dlmoFit = NULL,
  show_fit = FALSE,
  show_roi_heatmap = FALSE,
  plot_coarse = FALSE,
  mode = c("analyzed", "raw")
)
}
\arguments{
\item{profile_data}{A dataframe containing melatonin concentration data with \code{datetime} values.}

\item{show_threshold}{Logical. If \code{TRUE}, adds a \strong{horizontal threshold line} at \code{threshold} value.}

\item{threshold}{Numeric. The threshold value for melatonin concentration (default \verb{2.3 pg/mL}).}

\item{show_segments}{Logical. If \code{TRUE}, highlights \strong{base, intermediate, and ascending segments}.}

\item{show_parallelogram}{Logical. If \code{TRUE}, overlays a \strong{parallelogram} on the plot.}

\item{pll_result}{List containing parallelogram parameters, including:
\itemize{
\item \code{pll_datetime_0}: POSIXct timestamp for the left boundary.
\item \code{pll_datetime_1}: POSIXct timestamp for the right boundary.
\item \code{pll_slope}: Numeric. The slope of the parallelogram edges.
\item \code{corners}: List of corner coordinates (\code{ll}, \code{lr}, \code{ur}, \code{ul}).
}}

\item{show_roi}{Logical. If \code{TRUE}, overlays the \strong{region of interest (ROI)}.}

\item{roi_line_only}{Logical. If \code{TRUE}, only plots the \strong{ROI boundaries}, not the shaded area.}

\item{roi}{List containing ROI boundaries (\code{x_start}, \code{x_end}, \code{y_min}, \code{y_max}).}

\item{show_dlmoIP}{Logical. If \code{TRUE}, marks the \strong{DLMO inflection point}.}

\item{dlmoFit}{List containing \strong{DLMO fit results}, including:
\itemize{
\item \code{inflection_point}: A list with \code{x} (decimal hours) and \code{y} (melatonin level).
\item \code{base_params}: Parameters of the \strong{base segment fit}.
\item \code{ascending_params}: Parameters of the \strong{ascending segment fit}.
\item \code{grid}: search grid for inflection point.
\item \code{res}: Residuals from grid search.
}}

\item{show_fit}{Logical. If \code{TRUE}, overlays \strong{DLMO fit lines}.}

\item{show_roi_heatmap}{Logical. If \code{TRUE}, adds a \strong{heatmap} for \strong{ROI residuals}.}

\item{mode}{Character. Plot mode, either \code{"analyzed"} (default) for the
diagnostic DLMO plot or \code{"raw"} for a raw time-series plot before
segmentation and fitting. In \code{"raw"} mode, analysis overlays are disabled.
\code{profile_data} must already be a data frame; if starting from a CSV file,
read it first with \code{readr::read_delim(file_path, delim = ";")} or use
\code{\link[=plot_raw_profile]{plot_raw_profile()}} directly.}
}
\value{
A \code{ggplot2} object with the melatonin profile and optional overlays.
}
\description{
This function creates a plot of melatonin concentration over time with various
optional overlays, including \strong{DLMO fit lines, DLMO time stamp, threshold lines,}
\strong{melatonin profile segment highlights, best-fit parallelogram, and ROI residual heatmaps}.
}
\details{
\itemize{
\item This function \strong{plots melatonin concentration} over time with flexible overlays.
\item The \strong{DLMO fit} is visualized as a \strong{piecewise-linear or parabolic} fit.
\item The \strong{inflection point} is highlighted using a \strong{pink marker}.
\item A \strong{parallelogram} can be overlaid to show \strong{truncated ascending segments}.
\item The \strong{ROI heatmap} provides a \strong{residuals-based visualization} of the inflection search.
\item Use \code{mode = "raw"} to plot an unsegmented profile before running
\code{\link[=calculate_dlmo]{calculate_dlmo()}}. This is useful for checking threshold choice and
identifying possible early above-threshold excursions.
}
}
\examples{
filename <- system.file("extdata/sample_melatonin_profile.csv", package = "dlmoR")
raw_data <- readr::read_delim(filename, delim = ";", show_col_types = FALSE)

plot_profile(raw_data, threshold = 5, mode = "raw")

dlmo_result <- calculate_dlmo(file_path = filename, threshold = 5, fine_flag = FALSE)
dlmo_result$dlmoplotcoarse

plot_raw_profile(file_path = filename, threshold = 5)
}
\seealso{
\code{\link[=plot_raw_profile]{plot_raw_profile()}} for a convenience wrapper that accepts either a
data frame or a CSV file path.
}
