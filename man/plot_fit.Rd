% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_profile.R
\name{plot_fit}
\alias{plot_fit}
\title{Add DLMO Fit Lines to a Melatonin Profile Plot}
\usage{
plot_fit(plot, profile_data, dlmoFit)
}
\arguments{
\item{plot}{A ggplot object. The base plot of the melatonin profile.}

\item{profile_data}{A dataframe containing melatonin concentration data, with required columns:
\itemize{
\item \code{datetime} (POSIXct) – Time of measurement.
\item \code{melatonin} (numeric) – Melatonin concentration levels.
\item \code{base} (binary) – Indicator column for base segment points.
\item \code{ascending} (binary) – Indicator column for ascending segment points.
}}

\item{dlmoFit}{A list containing the estimated DLMO inflection point and fit parameters, including:
\itemize{
\item \code{inflection_point$x} (decimal hours) – The estimated x-coordinate of the inflection point.
\item \code{inflection_point$y} (numeric) – The estimated y-coordinate of the inflection point.
\item \code{base_params} (numeric) – Slope of the linear base fit.
\item \code{ascending_params} (list) – Either a single slope (for linear) or coefficients \verb{a, b, c} (for parabolic).
}}
}
\value{
A ggplot object with the fitted lines added.
}
\description{
This function overlays the estimated DLMO fit lines (base and ascending phase) onto a melatonin profile plot.
The fit can be either \strong{linear} or \strong{parabolic}, depending on the \code{dlmoFit} results.
}
\details{
\itemize{
\item The \strong{base fit} is always \strong{linear} and is drawn from the start of the base segment to the inflection point.
\item The \strong{ascending fit} can be either \strong{linear} or \strong{parabolic}:
\itemize{
\item \strong{Linear fit:} A simple straight line extending from the inflection point to the last ascending point.
\item \strong{Parabolic fit:} A nonlinear curve extending from the inflection point to the last ascending point.
}
\item Uses \code{posixct_to_decimal()} and \code{decimal_to_posixct()} to convert between time formats.
}
}
\examples{
\dontrun{
  plot <- ggplot2::ggplot(profile_data, ggplot2::aes(x = datetime, y = melatonin)) +
      ggplot2::geom_point()
  dlmoFit <- list(inflection_point = list(x = 20.5, y = 3.2),
                  base_params = 0.1,
                  ascending_params = list(a = -0.03, b = 2.1, c = 0.5))
  plot_fit(plot, profile_data, dlmoFit)
}
}
