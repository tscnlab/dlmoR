% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{seek_inflection}
\alias{seek_inflection}
\title{Seek the Inflection Point in a Melatonin Profile}
\usage{
seek_inflection(
  data,
  threshold = threshold,
  roi,
  step_x = 0.1,
  step_y = 0.2,
  step_size_small = 0.01,
  fit_type = "linear",
  fine_flag = fine_flag
)
}
\arguments{
\item{data}{A tibble containing melatonin profile data.}

\item{threshold}{Numeric. The threshold value for the fit constraint.}

\item{roi}{A list defining the \strong{region of interest} (ROI) for the search:
\itemize{
\item \code{x}: A numeric vector of two values representing the min and max x-coordinates.
\item \code{y}: A numeric vector of two values representing the min and max y-coordinates.
}}

\item{step_x}{Numeric. The step size for x-coordinates in the \strong{coarse grid} (default = 0.1).}

\item{step_y}{Numeric. The step size for y-coordinates in the \strong{coarse grid} (default = 0.2).}

\item{step_size_small}{Numeric. The step size for \strong{fine grid refinement} (default = 0.01).}

\item{fit_type}{Character. Type of fit used in \code{fit()}:
\itemize{
\item \code{"linear"} (default): Uses a linear model.
\item \code{"parabolic"}: Uses a parabolic model for the ascending segment.
}}

\item{fine_flag}{Logical. If \code{TRUE}, performs an additional \strong{fine-grid search} (default = \code{fine_flag}).}
}
\value{
A list containing:
\itemize{
\item \code{inflection_point}: The estimated inflection point (\code{x}, \code{y}).
\item \code{base_params}: Parameters of the \strong{base segment} fit.
\item \code{ascending_params}: Parameters of the \strong{ascending segment} fit.
\item \code{grid_big}: The coarse grid used in the first search.
\item \code{res_big}: The residuals corresponding to \code{grid_big}.
\item \code{grid_small}: The fine grid used in the second search (if \code{fine_flag = TRUE}).
\item \code{res_small}: The residuals corresponding to \code{grid_small} (if \code{fine_flag = TRUE}).
}
}
\description{
This function identifies the \strong{inflection point} in a melatonin profile by performing a
\strong{grid search} over a region of interest (ROI). It first conducts a \strong{coarse search} over
a large grid and, if \code{fine_flag = TRUE}, refines the search using a \strong{fine-grid search}.
}
\details{
\itemize{
\item The \strong{coarse grid search} is performed over the ROI.
\item If \code{fine_flag = TRUE}, the \strong{best 10\% of coarse-grid points} are used to refine the search.
\item The inflection point is selected as the point that \strong{minimizes the residual error} from \code{fit()}.
}
}
\seealso{
\code{\link{make_grid}}, \code{\link{reduce_grid}}, \code{\link{fit}}
}
