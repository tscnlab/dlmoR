% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{objective_function}
\alias{objective_function}
\title{Objective Function for Line or Parabola Fitting}
\usage{
objective_function(
  params,
  x,
  y,
  poi,
  fit_type,
  slope_bounds,
  base_id,
  region,
  weight_base,
  threshold = threshold
)
}
\arguments{
\item{params}{Numeric vector of parameters to optimize:
\itemize{
\item If \code{fit_type == "linear"}, \code{params[1]} is the slope (m).
\item If \code{fit_type == "parabolic"}, \code{params[1:2]} represent coefficients (a, b), and c is computed.
}}

\item{x}{Numeric vector of x-coordinates (time points in decimal format).}

\item{y}{Numeric vector of y-coordinates (melatonin concentrations).}

\item{poi}{List containing:
\itemize{
\item \code{x}: x-coordinate of the Point of Interest (POI).
\item \code{y}: y-coordinate of the POI.
}}

\item{fit_type}{Character. Either \code{"linear"} or \code{"parabolic"} to specify the fitting model.}

\item{slope_bounds}{Numeric vector of length 2 defining the allowed slope range.}

\item{base_id}{Numeric vector indicating which points belong to the base segment (1 = base, 0 = ascending).}

\item{region}{Character. Either \code{"base"} or \code{"ascending"} to specify the segment being fitted.}

\item{weight_base}{Logical. If \code{TRUE}, assigns a lower weight to base points in the residual computation.}

\item{threshold}{Numeric. Upper bound constraint for base fitting. Default is \code{threshold}.}
}
\value{
Numeric value representing the total cost (sum of squared residuals and constraint penalties).
}
\description{
This function calculates the cost for fitting either a linear or parabolic model
to a segment of the melatonin profile. The objective minimizes the residual error
while ensuring the fit adheres to predefined constraints.
}
\details{
\itemize{
\item The function supports both linear and parabolic fits.
\item The \code{base_constraint} function penalizes fits that exceed the threshold in the base region.
\item The \code{nl_constraints} function ensures that parabolic fits maintain an increasing slope.
\item The \code{poi_constraints} function ensures that the fit slope at the POI stays within the given bounds.
\item Weighted residuals are applied to emphasize ascending points in parabolic fitting.
}
}
\examples{
x <- c(0, 1, 2, 3, 4)
y <- c(0.5, 1.0, 2.0, 3.5, 5.0)
poi <- list(x = 2, y = 2.0)
params <- c(1.5)  # Linear slope example
slope_bounds <- c(-2, 2)
objective_function(params, x, y, poi, "linear", slope_bounds, base_id = c(1,1,0,0,0), "ascending", weight_base = TRUE)

}
\seealso{
\code{\link{base_constraint}}, \code{\link{nl_constraints}}, \code{\link{poi_constraints}}
}
