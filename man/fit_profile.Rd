% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{fit_profile}
\alias{fit_profile}
\title{Fit a Melatonin Profile to Base or Ascending Segment}
\usage{
fit_profile(
  x,
  y,
  poi,
  fit_type = "linear",
  region = "base",
  base_tangent = NULL,
  ascending_linear_tangent = NULL,
  base_id = NULL,
  weight_base = TRUE,
  threshold = threshold
)
}
\arguments{
\item{x}{Numeric vector of x-coordinates (decimal time).}

\item{y}{Numeric vector of y-coordinates (melatonin concentration).}

\item{poi}{List containing:
\itemize{
\item \code{x}: The x-coordinate of the Point of Interest (POI).
\item \code{y}: The y-coordinate of the POI.
}}

\item{fit_type}{Character. \code{"linear"} (default) or \code{"parabolic"} for the type of fit.}

\item{region}{Character. \code{"base"} (default) or \code{"ascending"} to specify which part of the profile to fit.}

\item{base_tangent}{Numeric. Estimated slope of the base segment (used if \code{fit_type = "parabolic"}).}

\item{ascending_linear_tangent}{Numeric. Estimated slope of the ascending segment (used if \code{fit_type = "parabolic"}).}

\item{base_id}{Numeric vector. Indicator variable where \code{1} represents base points and \code{0} represents ascending points.}

\item{weight_base}{Logical. If \code{TRUE}, applies lower weights to base points in residual computation.}

\item{threshold}{Numeric. Upper boundary for the base fit. Default is \code{threshold}.}
}
\value{
A list containing:
\itemize{
\item \code{residual}: The mean squared residual error.
\item \code{params}: The fitted model parameters.
}
}
\description{
This function fits a linear or parabolic model to either the \strong{base} or \strong{ascending}
segment of a melatonin profile relative to a specified \strong{point of interest (POI)}.
It ensures that the fit adheres to predefined constraints.
}
\details{
\itemize{
\item \strong{Base segment fitting (\code{region = "base"})}: Uses a \strong{linear} fit constrained within slope bounds.
\item \strong{Ascending segment fitting (\code{region = "ascending"})}:
\itemize{
\item If \code{fit_type = "linear"}, a \strong{linear fit} is applied with no slope constraints.
\item If \code{fit_type = "parabolic"}, a \strong{quadratic fit} is optimized while enforcing \strong{monotonicity}.
}
\item The function \strong{penalizes} fits that do not meet slope constraints.
\item \strong{Weighted residuals} prioritize \strong{ascending points} when computing the fit error.
}
}
\examples{
x <- c(0, 1, 2, 3, 4)
y <- c(0.5, 1.0, 2.0, 3.5, 5.0)
poi <- list(x = 2, y = 2.0)
fit_profile(x, y, poi, fit_type = "linear", region = "ascending", weight_base = TRUE)

}
\seealso{
\code{\link{fit_linear}}, \code{\link{objective_function}}
}
