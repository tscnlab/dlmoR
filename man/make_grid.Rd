% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{make_grid}
\alias{make_grid}
\title{Generate a Grid of Points Within a Region of Interest}
\usage{
make_grid(roi, step_x, step_y)
}
\arguments{
\item{roi}{A list containing:
\itemize{
\item \code{x}: A numeric vector of length 2 specifying the x-axis range (\code{xmin}, \code{xmax}).
\item \code{y}: A numeric vector of length 2 specifying the y-axis range (\code{ymin}, \code{ymax}).
}}

\item{step_x}{Numeric. The step size for x-coordinates (grid resolution along the x-axis).}

\item{step_y}{Numeric. The step size for y-coordinates (grid resolution along the y-axis).}
}
\value{
A data frame containing all grid points with columns:
\itemize{
\item \code{x}: The x-coordinates of the grid points.
\item \code{y}: The y-coordinates of the grid points.
}
}
\description{
This function generates a uniform grid of points within a specified region of interest (ROI).
The grid is created based on user-defined step sizes for the x and y dimensions.
}
\examples{
roi <- list(x = c(1, 5), y = c(2, 6))
grid <- make_grid(roi, step_x = 0.5, step_y = 0.5)
print(grid)

}
\seealso{
\code{\link{seek_inflection}}, which utilizes this function to generate search grids.
}
