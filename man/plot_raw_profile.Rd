% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_profile.R
\name{plot_raw_profile}
\alias{plot_raw_profile}
\title{Plot Raw Melatonin Profile}
\usage{
plot_raw_profile(data = NULL, file_path = NULL, threshold = NULL)
}
\arguments{
\item{data}{Optional data frame containing \code{datetime} and \code{melatonin} columns.}

\item{file_path}{Optional path to a CSV file containing \code{datetime} and
\code{melatonin} columns. Used when \code{data} is \code{NULL}.}

\item{threshold}{Optional numeric threshold to draw as a horizontal reference
line. If \code{NULL}, no threshold line is drawn.}
}
\value{
A \code{ggplot2} object showing the raw melatonin time series.
}
\description{
Creates a simple time-series plot of melatonin concentration before DLMO
segmentation or fitting. This is useful for checking timestamp order, the
approximate timing of threshold crossings, and whether a chosen threshold is
likely to identify the intended melatonin rise.
}
\examples{
filename <- system.file("extdata/sample_melatonin_profile.csv", package = "dlmoR")
plot_raw_profile(file_path = filename, threshold = 5)

raw_data <- readr::read_delim(filename, delim = ";")
plot_raw_profile(data = raw_data)

}
\seealso{
\code{\link[=plot_profile]{plot_profile()}} with \code{mode = "raw"} for the underlying plotting
method.
}
