% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{fit}
\alias{fit}
\title{Fit Two Splines Around a Point of Interest (POI)}
\usage{
fit(data, poi, fit_type = "linear", threshold = threshold)
}
\arguments{
\item{data}{A tibble containing melatonin concentration data with \code{datetime} values.}

\item{poi}{A list containing:
\itemize{
\item \code{x}: The x-coordinate (decimal time) of the POI.
\item \code{y}: The y-coordinate (melatonin concentration) of the POI.
}}

\item{fit_type}{Character. Type of fit for the ascending segment:
\itemize{
\item \code{"linear"} (default): Uses a linear model for both base and ascending segments.
\item \code{"parabolic"}: Uses a \strong{parabolic fit} for the ascending segment.
}}

\item{threshold}{Numeric. The threshold for constraining the base segment.}
}
\value{
A list containing:
\itemize{
\item \code{residual}: The sum of squared residuals from both fits.
\item \code{base_params}: The estimated parameters for the base fit.
\item \code{ascending_params}: The estimated parameters for the ascending fit.
}
}
\description{
This function fits a \strong{piecewise linear or parabolic model} to melatonin profile data.
It separately models the \strong{base segment} and the \strong{ascending segment}, ensuring a smooth transition at the \strong{point of interest (POI)}.
}
\details{
\itemize{
\item The \strong{base segment} is always fit using a \strong{linear model}.
\item The \strong{ascending segment} is fit using a \strong{linear} or \strong{parabolic} model depending on \code{fit_type}.
\item If the \strong{ascending segment} has at least \strong{3 data points}, it first fits a linear model and \strong{then refines it} with a parabolic fit.
\item This function is used in \strong{inflection point detection}.
}
}
\examples{
data <- tibble::tibble(
  datetime = as.POSIXct(c("2024-06-01 22:00", "2024-06-01 22:15", "2024-06-01 22:30")),
  melatonin = c(0.5, 0.8, 1.2),
  base = c(1, 1, 0)  # Base segment indicators
)
poi <- list(x = posixct_to_decimal(data$datetime[2], data$datetime), y = data$melatonin[2])
fit(data, poi, fit_type = "linear", threshold = 2.3)

}
\seealso{
\code{\link{fit_profile}}, \code{\link{objective_function}}, \code{\link{seek_inflection}}
}
