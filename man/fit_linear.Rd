% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{fit_linear}
\alias{fit_linear}
\title{Perform a Weighted Linear Fit}
\usage{
fit_linear(
  x,
  y,
  poi,
  base_id = NULL,
  slope_bounds = NULL,
  edge_bounds = NULL,
  weight_base = TRUE
)
}
\arguments{
\item{x}{Numeric vector of x-coordinates (decimal time).}

\item{y}{Numeric vector of y-coordinates (melatonin concentration).}

\item{poi}{List containing:
\itemize{
\item \code{x}: The x-coordinate of the Point of Interest (POI).
\item \code{y}: The y-coordinate of the POI.
}}

\item{base_id}{Numeric vector. Indicator variable where \code{1} represents base points and \code{0} represents ascending points.}

\item{slope_bounds}{Numeric vector of length 2 specifying lower and upper bounds for the slope. Default is \code{NULL} (no constraints).}

\item{edge_bounds}{List containing:
\itemize{
\item \code{left}: Numeric vector \verb{[min, max]} range for left boundary (base segment).
\item \code{right}: Numeric vector \verb{[min, max]} range for right boundary (ascending segment).
If \code{NULL}, no edge constraints are applied.
}}

\item{weight_base}{Logical. If \code{TRUE}, applies lower weights to base points in residual computation.}
}
\value{
A list containing:
\itemize{
\item \code{params}: A numeric vector with the estimated slope.
}
}
\description{
This function fits a \strong{linear model} to a given segment of a melatonin profile.
It estimates the slope of the best-fit line while enforcing \strong{slope constraints}
and \strong{edge boundary conditions} if specified.
}
\details{
\itemize{
\item The function \strong{applies weights} to prioritize ascending points over base points.
\item \strong{Slope constraints} ensure that the estimated slope remains within predefined bounds.
\item If \strong{edge constraints} are provided, the function ensures that the fitted line \strong{does not exceed} specified y-limits at the segment edges.
}
}
\examples{
x <- c(0, 1, 2, 3, 4)
y <- c(0.5, 1.0, 2.0, 3.5, 5.0)
poi <- list(x = 2, y = 2.0)
base_id <- c(1, 1, 0, 0, 0)
fit_linear(x, y, poi, base_id = base_id, slope_bounds = c(-0.2, 0.2), weight_base = TRUE)

}
\seealso{
\code{\link{fit_profile}}, \code{\link{objective_function}}
}
