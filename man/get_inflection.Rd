% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{get_inflection}
\alias{get_inflection}
\title{Identify the Inflection Point in a Melatonin Profile}
\usage{
get_inflection(
  profile_data,
  threshold = 2.3,
  posix_roi,
  fit_type = "linear",
  fine_flag = TRUE
)
}
\arguments{
\item{profile_data}{A tibble containing melatonin profile data with columns:
\itemize{
\item \code{datetime} (POSIXct): The timestamp of each measurement.
\item \code{melatonin} (numeric): The melatonin concentration.
\item \code{base} (binary): Indicator for the baseline segment.
\item \code{ascending} (binary): Indicator for the ascending segment.
\item \code{intermediate} (optional, binary): Indicator for an intermediate segment.
}}

\item{threshold}{Numeric. The threshold value for constraining the fit (default = 2.3).}

\item{posix_roi}{A list defining the \strong{region of interest} (ROI) using POSIXct timestamps:
\itemize{
\item \code{x_start}: Start time of the ROI.
\item \code{x_end}: End time of the ROI.
\item \code{y_min}: Minimum melatonin concentration in the ROI.
\item \code{y_max}: Maximum melatonin concentration in the ROI.
}}

\item{fit_type}{Character. Specifies the type of fit used:
\itemize{
\item \code{"linear"} (default): Uses a linear model.
\item \code{"parabolic"}: Uses a parabolic model for the ascending segment.
}}

\item{fine_flag}{Logical. If \code{TRUE}, enables \strong{fine-grid refinement} in \code{seek_inflection()} (default = \code{TRUE}).}
}
\value{
A list containing:
\itemize{
\item \code{inflection_point}: The estimated inflection point (\code{x}, \code{y}).
\item \code{base_params}: Parameters of the \strong{base segment} fit.
\item \code{ascending_params}: Parameters of the \strong{ascending segment} fit.
\item \code{grid_big}: The coarse grid used in the first search.
\item \code{res_big}: The residuals corresponding to \code{grid_big}.
\item \code{grid_small}: The fine grid used in the second search (if \code{fine_flag = TRUE}).
\item \code{res_small}: The residuals corresponding to \code{grid_small} (if \code{fine_flag = TRUE}).
}
}
\description{
This function determines the \strong{inflection point} in a melatonin profile by calling
\code{seek_inflection()}, which performs a grid search over a \strong{region of interest (ROI)}.
}
\details{
\itemize{
\item The \strong{ROI is converted} from POSIXct format to decimal time using \code{posixct_to_decimal()}.
\item If the \code{profile_data} contains an \code{"intermediate"} segment, it is \strong{included} in the fit.
\item Calls \code{seek_inflection()} to identify the best-fit \strong{inflection point}.
}
}
\seealso{
\code{\link{seek_inflection}}, \code{\link{posixct_to_decimal}}, \code{\link{fit}}
}
