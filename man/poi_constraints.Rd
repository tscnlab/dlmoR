% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{poi_constraints}
\alias{poi_constraints}
\title{Enforce Slope Constraints at the Point of Interest (POI)}
\usage{
poi_constraints(params, slope_bounds, poi_x, fit_type = "linear")
}
\arguments{
\item{params}{A numeric vector containing the model parameters:
\itemize{
\item For a linear fit: \code{params[1]} is the slope.
\item For a parabolic fit: \code{params[1]} (quadratic coefficient) and \code{params[2]} (linear coefficient).
}}

\item{slope_bounds}{A numeric vector of length 2 specifying the lower and upper slope limits.}

\item{poi_x}{Numeric. The x-coordinate of the Point of Interest.}

\item{fit_type}{Character. Specifies the type of fit used. Can be \code{"linear"} or \code{"parabolic"}.}
}
\value{
A numeric value:
\itemize{
\item \code{0} if the computed slope is within bounds.
\item A squared penalty value if the computed slope is outside the allowable range.
}
}
\description{
This function ensures that the slope at the Point of Interest (POI) falls within the specified bounds.
It applies different calculations depending on whether the fit is linear or parabolic.
}
\details{
\itemize{
\item If the fit type is \code{"linear"}, the slope is taken directly from \code{params[1]}.
\item If the fit type is \code{"parabolic"}, the slope at the POI is computed as \eqn{2a \cdot poi_x + b}.
\item The function checks if the computed slope falls within the specified \code{slope_bounds}.
\item If the slope is out of bounds, it applies a squared penalty to encourage corrections during optimization.
}
}
\examples{
params_linear <- c(0.5)  # Example slope for a linear fit
slope_bounds <- c(0.2, 1.0)
poi_x <- 5
poi_constraints(params_linear, slope_bounds, poi_x, fit_type = "linear")

params_parabolic <- c(0.1, 0.5)  # Example quadratic and linear coefficients
poi_constraints(params_parabolic, slope_bounds, poi_x, fit_type = "parabolic")

}
\seealso{
\code{\link{objective_function}} which utilizes this constraint during optimization.
}
