% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{nl_constraints}
\alias{nl_constraints}
\title{Nonlinear Constraints for Parabolic Fitting}
\usage{
nl_constraints(params, poi_x, poi_y, x)
}
\arguments{
\item{params}{A numeric vector of length 3 representing the parabola parameters:
\itemize{
\item \code{a}: Quadratic coefficient.
\item \code{b}: Linear coefficient.
\item \code{c}: Constant term.
}}

\item{poi_x}{Numeric. The x-coordinate of the point of interest (POI).}

\item{poi_y}{Numeric. The y-coordinate of the POI.}

\item{x}{Numeric vector of x-values representing the ascending segment.}
}
\value{
A list containing:
\itemize{
\item \code{pass_poi}: The squared deviation from the constraint that the parabola must pass through the POI.
\item \code{pos_grad}: The squared deviation from the constraint ensuring a positive slope at the last ascending point.
}
}
\description{
This function enforces constraints on a parabolic fit for ascending melatonin concentration data.
The constraints ensure that the fitted parabola passes through a specified point of interest (POI)
and maintains a positive slope in the ascending segment.
}
\details{
The function imposes two constraints:
\enumerate{
\item \strong{Passing through POI}: Ensures the fitted parabola satisfies \eqn{y = ax^2 + bx + c} at \verb{poi_x, poi_y}.
\item \strong{Positive Slope}: Ensures \eqn{\frac{dy}{dx} = 2ax + b} remains non-negative at the last x-value.
}

If the minimum slope at the last ascending point is negative, it is forced to zero as a penalty.
}
\examples{
params <- c(0.1, 2, 3)  # Example parabola: y = 0.1x^2 + 2x + 3
poi_x <- 5
poi_y <- 15
x_values <- seq(4, 6, by = 0.5)
constraints <- nl_constraints(params, poi_x, poi_y, x_values)
print(constraints)

}
\seealso{
\code{\link{objective_function}}, which utilizes these constraints during optimization.
}
