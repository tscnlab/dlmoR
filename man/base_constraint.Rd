% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek_inflection.R
\name{base_constraint}
\alias{base_constraint}
\title{Constraint Function for Base Fit Edge Alignment}
\usage{
base_constraint(y, min_y, poi_y, threshold = 2.3)
}
\arguments{
\item{y}{Numeric vector representing the fitted y-values along the base segment.}

\item{min_y}{Numeric. The minimum y-value in the dataset (used as a lower bound).}

\item{poi_y}{Numeric. The y-value at the Point of Interest (POI).}

\item{threshold}{Numeric. The upper y-bound for the base fit. Default is \code{2.3}.}
}
\value{
A numeric penalty value:
\itemize{
\item \code{0} if both edges are within the defined range.
\item A squared penalty if the left or right edges exceed their respective bounds.
}
}
\description{
This function ensures that the base fit remains within defined vertical bounds
by applying penalties if the left or right edge of the base segment exceeds limits.
}
\details{
\itemize{
\item The function applies constraints separately for the left and right edges of the base segment.
\item The left constraint ensures that the starting y-value remains within \verb{[0, threshold]}.
\item The right constraint ensures that the POI y-value is between \verb{[min_y, threshold]}.
\item If either constraint is violated, a squared penalty is applied to encourage corrections during optimization.
}
}
\examples{
y_values <- c(0.5, 1.2, 2.0)  # Example y-values for the base segment
min_y <- 0.1
poi_y <- 1.8
threshold <- 2.3
base_constraint(y_values, min_y, poi_y, threshold)

}
\seealso{
\code{\link{objective_function}} where this function is used in the optimization process.
}
